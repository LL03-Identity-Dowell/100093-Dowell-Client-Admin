// interface NotificationItem {
//   username: string;
//   product: string;
// }

export interface NotificationsListProps {
  notifications?: {
    notifications: {
      username: string;
  product: string;
    }
  };
  // removeNotifications: (product: string) => void;
}